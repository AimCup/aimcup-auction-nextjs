# Copy to .env for local development. In deployment the API origin is baked at build time via the
# NEXT_PUBLIC_API_URL build arg in docker-compose.yml / docker-compose.next.yml (not from .env).
NEXT_PUBLIC_API_URL=http://localhost:8080
